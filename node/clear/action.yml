name: GitHub action clear for pipeline in Node.js
description: GitHub action clear for pipeline in Node.js

runs:
  using: "composite"
  steps: 

    ############# Clear #############

    - name: Clean temporary build artifacts
      run: ./clean.ps1
      shell: bash
    