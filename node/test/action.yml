name: GitHub action test for pipeline in Node.js
description: GitHub action test for pipeline in Node.js

runs:
  using: "composite"
  steps: 

    ############# Test #############

    - name: Test the component
      run: ./test.ps1
      shell: bash
    