name: GitHub action build for pipeline in Node.js
description: GitHub action build for pipeline in Node.js

runs:
  using: "composite"
  steps: 

    ############# Build #############
    
    - name: Build the component
      run: ./build.ps1
      shell: bash
