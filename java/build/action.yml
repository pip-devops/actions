name: GitHub action build for pipeline in Java

runs:
  using: "composite"
  steps: 

    ############# Build #############
    
    - name: Build the component
      run: ./build.ps1
      shell: bash
