name: GitHub action test for pipeline in Java
description: GitHub action test for pipeline in Java

runs:
  using: "composite"
  steps: 

    ############# Test #############

    - name: Test the component
      run: ./test.ps1
      shell: bash
    