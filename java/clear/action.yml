name: GitHub action clear for pipeline in Java
description:  GitHub action clear for pipeline in Java

runs:
  using: "composite"
  steps: 

    ############# Clear #############

    - name: Clean temporary build artifacts
      run: ./clean.ps1
      shell: bash
    