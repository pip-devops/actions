name: GitHub action build for pipeline in .NET

runs:
  using: "composite"
  steps: 

    ############# Build #############
    
    - name: Build the component
      run: ./build.ps1
      shell: bash
