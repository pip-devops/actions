name: GitHub action test for pipeline in Go
description: GitHub action test for pipeline in Go

runs:
  using: "composite"
  steps: 

    ############# Test #############

    - name: Test the component
      run: ./test.ps1
      shell: bash
    